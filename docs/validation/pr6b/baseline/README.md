# PR6B baseline

Las capturas históricas previas a PR6B son las referencias visuales adjuntas al análisis técnico. La automatización puede materializar una baseline reproducible con:

```bash
PR6B_ARTIFACT_SET=baseline npm run test:visual
```

No se inventa una baseline posterior al cambio: los candidatos generados por esta implementación se guardan en `../candidate`.
